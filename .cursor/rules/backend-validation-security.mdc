---
description: 后端校验与安全（Zod/敏感数据/安全中间件）
globs: dating_app_api/src/schemas/**/*.ts,dating_app_api/src/middleware/**/*.ts,dating_app_api/src/routes/**/*.ts,dating_app_api/src/controllers/**/*.ts
alwaysApply: false
---

# 校验与安全规范（强制）

## 输入校验（必须）

- 所有用户输入必须校验（`unknown` → Zod parse），失败返回 400
- 校验尽量在 Route/Controller 入口处完成（提前拒绝）

## 敏感数据（强制）

- **禁止硬编码**密钥/连接串；必须走环境变量，并对 env 做 schema 校验
- 密码必须 hash（例如 bcrypt）；响应/日志必须过滤敏感字段（如 `passwordHash`）

## 常见安全项（按需）

- 合理配置 CORS、rate limit、helmet、安全头
- 避免 XSS：对用户生成 HTML 做 sanitize（若项目有该场景）
